import 'package:flutter/material.dart';

class AppColors {
  // Старые цвета (используются в app_theme.dart, main_nav_shell.dart, dashboard_screen.dart)
  static const Color primaryBackground = Color(0xFF1A1A2E);
  static const Color cardBackground = Color(0xFF252541);
  static const Color lightCardBackground = Color(0xFFFFFFFF);
  static const Color lightPrimaryBackground = Color(0xFFF5F5F5);

  // Текст — тёмная тема
  static const Color primaryText = Colors.white;
  static const Color secondaryText = Colors.white70;
  static const Color hintTextColor = Colors.white38;
  static const Color disabledTextColor = Colors.white30;

  // Текст — светлая тема
  static const Color lightPrimaryText = Color(0xFF1A1A2E);
  static const Color lightSecondaryText = Color(0xFF666666);
  static const Color lightHintTextColor = Color(0xFF999999);
  static const Color lightDisabledTextColor = Color(0xFFCCCCCC);

  // Акцентные цвета
  static const Color primaryBlue = Color(0xFF2196F3);
  static const Color accentGreen = Colors.green;
  static const Color accentRed = Colors.red;
  static const Color accentOrange = Colors.orange;

  // Границы
  static const Color primaryBorder = Colors.white30;
  static const Color lightPrimaryBorder = Color(0xFFE0E0E0);
  static const Color focusedBorder = Color(0xFF2196F3);
  static const Color successBorder = Colors.green;

  // Статусы
  static const Color onlineStatus = Colors.green;
  static const Color offlineStatus = Colors.red;
  static const Color pendingStatus = Colors.orange;

  // Новые цвета HAI3 (добавлены в текущей версии)
  static const Color primary = Color(0xFF1A1A1A);
  static const Color accent = Color(0xFF2196F3);
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F5);
  static const Color onSurface = Color(0xFF1A1A1A);
  static const Color lightBlue = Color(0xFFE3F2FD);
  static const Color border = Color(0xFFEEEEEE);
  static const Color greyText = Color(0xFF757575);
}